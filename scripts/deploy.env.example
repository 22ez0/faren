# Copie para a raiz do repositorio como .env.deploy (o ficheiro .env.deploy esta no .gitignore)
# NUNCA commite chaves reais.

RENDER_API_KEY=
GITHUB_TOKEN=

# Opcional: se o service id mudar no Render
# RENDER_SERVICE_ID=srv-d7gjdc5ckfvc73ftk79g
